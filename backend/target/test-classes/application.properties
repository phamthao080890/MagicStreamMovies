# ?? Test-only overrides ??????????????????????????????????????????????????????
# Use a fake URI so no real MongoDB connection is attempted during unit tests.
# The MongoClient / MongoDatabase beans are mocked at the test-class level for
# full-context tests (@SpringBootTest), so this URI is never dialled.
spring.data.mongodb.uri=mongodb://localhost:27017/testdb
spring.data.mongodb.database=testdb

server.port=0

